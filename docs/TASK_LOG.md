# Task Log

## 2026-06-19 - Clock cancellation-fee accounting design boundary
- Phase status: RESEARCH COMPLETE; no production code, tests, database schema, or provider writes changed.
- Files changed: `docs/CLOCK_CANCELLATION_FEE_ACCOUNTING_DESIGN.md`, `docs/DECISIONS.md`, `docs/TASK_LOG.md`.
- Findings: Investigated the cached Clock charge-create and reread contracts for booking `charges_by_source`, generic folio charges, booking/folio rereads, and the current cancellation, refund, folio, accounting, pricing-reconciliation, provider-log, admin, and focused test paths. The plugin currently stores immutable cancellation snapshots plus payment/refund credit-item accounting evidence, but it does not store the retained-fee charge id, ownership markers, folio-selection decision, cleanup prerequisite confirmation, or charge-level idempotency required to post a retained cancellation fee safely.
- Outcome: `BLOCKED — CANCELLATION-FEE ACCOUNTING MUST REMAIN MANUAL`. There are no safe automatic retained-fee posting cases with the current documented Clock contracts and current stored data because duplicate accommodation revenue cannot be ruled out.
- Commands/checks run: baseline `git status --short`, `git branch --show-current`, `git rev-parse HEAD`, plugin version read, `git diff --check`, targeted docs/code searches, raw local collection extraction, and final doc-only checks.
- External records created: None. No Clock API requests, provider writes, commits, pushes, tags, packaging, or releases were performed.

## 2026-06-19 - Clock accommodation-charge cleanup design boundary
- Phase status: RESEARCH COMPLETE; no production code, tests, database schema, or provider writes changed.
- Files changed: `docs/CLOCK_ACCOMMODATION_CHARGE_CLEANUP_DESIGN.md`, `docs/DECISIONS.md`, `docs/TASK_LOG.md`.
- Findings: Investigated the cached Clock contracts for charge delete, folio `charges/bulk`, and booking `charges_by_source`, then cross-checked the current cancellation, refund, folio, accounting, request-log, and amendment code paths. The plugin currently stores payment/refund folio accounting and cancellation snapshots, but not charge-level Clock ids, ownership markers, template ids, or cleanup attempt records needed to prove that an accommodation charge is safe to reverse automatically.
- Outcome: `BLOCKED - MANUAL CLEANUP MUST REMAIN`. Automatic accommodation-charge cleanup and automatic Clock cancellation-fee posting must stay manual until Clock confirms the missing contract details and the plugin adds a dedicated cleanup persistence layer.
- Commands/checks run: baseline `git status --short`, `git branch --show-current`, `git rev-parse HEAD`, plugin version read, `git diff --check`, targeted research/code searches, raw local collection extraction, and final doc-only checks.
- External records created: None. No Clock API requests, provider writes, commits, pushes, tags, packaging, or releases were performed.

## 2026-06-19 - Clock live public export refresh
- Phase status: RESEARCH COMPLETE; no Clock API requests were sent and no Postman write operations were performed.
- Files changed: `tmp/vendor-docs/clock/Clock PMS+ API Docs.postman_collection.json`, `docs/research/clock/postman-endpoint-index.md`, `docs/research/clock/SOURCE_MANIFEST.md`, `docs/INTEGRATIONS.md`, `docs/DECISIONS.md`, `docs/TASK_LOG.md`.
- Findings: Refreshed the ignored local Clock cache from the live public export (`_postman_id` `d2cd8fbb-53e9-43cd-a4db-5f5757094634`, SHA-256 `32F27C40B444A02EBB576A0D97BE1B5C2B31690B9302EF3499AF333C70535364`). The live changelog is newer (`2026-06-12`) and adds `booking_charges_by_source - BULK CREATE`, with the POS guide updated to use the booking-associated bulk endpoint. Checked-in/current-room and future scheduled room moves remain blocked; accommodation-charge cleanup, cancellation-fee accounting, and discount synchronization remain partially documented.
- Commands/checks run: SHA-256 validation, BOM-safe JSON parsing, live search-term sweep across request names/folder names/descriptions/URLs/bodies/examples, compare summary, `git status --short`, and `git diff --check`.
- External records created: None. The refreshed raw Postman export remains in ignored `tmp/` vendor-doc cache only.

## 2026-06-19 - Clock Postman collection indexing and blocker re-evaluation
- Phase status: RESEARCH COMPLETE; no production code or tests changed.
- Files changed: `.gitignore`, `docs/research/clock/postman-endpoint-index.md`, `docs/research/clock/SOURCE_MANIFEST.md`, `docs/INTEGRATIONS.md`, `docs/DECISIONS.md`, `docs/TASK_LOG.md`.
- Findings: Cached the supplied Clock PMS+ Postman collection under ignored `tmp/vendor-docs/clock/`, indexed 192 HTTP requests across 52 folder nodes, and detected 12 deprecated requests plus 25 duplicate method/path signatures that mostly come from guide/example aliases.
- Clarified support boundaries: Checked-in/current-room moves and future scheduled room changes remain blocked on read-only evidence (`current_room_id`, `booking_room_changes`). The full collection newly documents generic charge voiding, compensating negative-quantity charge writes, booking-associated charge creation, and discount charge examples, so accommodation-charge cleanup, cancellation-fee accounting, and discount mirroring are now tracked as partially documented rather than completely undocumented.
- Commands/checks run: Collection JSON parse/validity check, SHA-256 hash, recursive endpoint extraction, duplicate/deprecated detection, secret/privacy scan, `git status --short`, and `git diff --check`.
- External records created: None. The raw Postman export remains in ignored `tmp/` vendor-doc cache only.

## 2026-06-18 - Release candidate preparation for 0.4.82
- Phase status: READY FOR RELEASE CANDIDATE PACKAGE; not production-ready. Recommend a GitHub pre-release, not a final production release.
- Files changed: `must-hotel-booking.php`, `readme.txt`, `docs/TASK_LOG.md`.
- Findings: Latest Git tag is `v0.4.81` and the worktree version before this pass was `0.4.81`. The bundled GitHub updater always skips drafts and, with the current configuration, also skips GitHub pre-releases. The established release asset name remains `must-hotel-booking-{version}.zip`.
- Fixes completed: Bumped the plugin header, version constant, and readme stable tag to `0.4.82`. Added a `0.4.82` changelog entry covering Clock inbound retry and deduplication hardening, room move and pre-arrival room-type amendments, cancellation/refund lifecycle hardening, Clock payment/refund accounting improvements, and the remaining Clock acceptance limitations. Built a clean release ZIP `must-hotel-booking-0.4.82.zip` with docs, tests, tools, tmp, VCS metadata, local env files, logs, and other development-only artifacts excluded.
- Commands/tests run: targeted release metadata reads; Git tag inspection; updater library inspection; local package build; ZIP structure and entry verification; package secret scan; `php -l must-hotel-booking.php`; `git diff --check`.
- Exact results: Release ZIP `must-hotel-booking-0.4.82.zip` contains 339 entries, all under `must-hotel-booking/`, with `must-hotel-booking/must-hotel-booking.php` at the ZIP root. Required new production classes and `includes/autoloader.php` are present. The packaged plugin file reports version `0.4.82` in both the header and `MUST_HOTEL_BOOKING_VERSION`. SHA-256 is `994D3EECE4F4CFFA27E7363BFADD85B1BED60D28CF11243E2A5E1B8380F3FE84`. The staged package secret scan found no literal credentials or private keys.
- External records created: None.
- Remaining risks/blockers: Real Clock SNS acceptance is still blocked. Real Clock amendment acceptance is still blocked. Checked-in Clock room moves remain unsupported. Clock accommodation-charge cleanup may still require manual handling. Do not describe this candidate as fully production-ready.

## 2026-06-18 - Room move/upgrade and Clock room-type amendment
- Phase status: PASS for local physical-room move logic; CODE READY - EXTERNAL ACCEPTANCE BLOCKED for pre-arrival Clock assignment/type/date amendments; BLOCKED for checked-in Clock current-room moves.
- Fixes completed: Added the shared amendment service, conflict-aware local destination update, local/admin room assignment, combined room/type/rate/date forms, Clock GET -> PUT -> GET confirmation, reread-first retries, reconciliation jobs, manual upgrade/downgrade review, and explicit checked-in Clock blocking.
- Critical corrections: Restored pre-arrival local unassignment, required both stay and room capabilities for combined staff amendments, hid unsupported checked-in Clock move controls, rejected inactive/unbookable destination rooms, and preserved financial-review metadata on duplicate Clock retries.
- Checks: Focused amendment tests passed; Clock inbound service/SNS tests passed; lifecycle smoke passed; accounting readiness passed; read-only production lifecycle harness exited `0` with expected write scenarios `BLOCKED`; PHP lint and diff checks passed.
- Rendered QA: Blocked by the browser security policy for the configured localhost URL; no form submission or browser workaround was attempted.
- External records/settings changed: None.
- Acceptance evidence: `docs/RESERVATION_AMENDMENT_ACCEPTANCE.md`.

## 2026-06-18 - Clock inbound PUSH/SNS production audit and retry hardening
- Phase status: BLOCKED for production acceptance; local code and replay verification passed.
- Root cause: The inbound adapter could acknowledge and permanently deduplicate a Clock SNS booking event after a transient booking-detail fetch failure without durably applying a local update. The inspected site also generates an ngrok callback while the requested production host timed out.
- Fixes completed: Added retryable failure semantics, official status-subject fallbacks, database advisory locking, idempotent subscription confirmation, strict complete-pair Basic auth, reverse-proxy auth fallback, explicit nested JSON rejection, sanitized logging assertions, and readiness diagnostics.
- Checks: PHP lint passed; Clock SNS replay and inbound service tests passed; lifecycle smoke returned `PASS`; read-only E2E exited `0` with `overall_status=BLOCKED` because no real SNS replay was run; diff/ignore/secret scans passed.
- Public verification: Configured ngrok route returned controlled unsigned `403` with `Allow: POST`; expected production host timed out.
- External records/settings changed: None.
- Remaining blocker: Real signed Clock SNS subscription confirmation, notification, duplicate replay, and Clock/WordPress reread comparison on an approved disposable booking.

## 2026-06-14 - Clock inbound PUSH/SNS compatibility
- Phase status: PASS for local inbound webhook replay tests; production Clock inbound replay still requires configuring the real Clock PUSH endpoint and replaying production/sandbox Clock SNS notifications.
- Files changed: `src/Provider/Clock/ClockInboundSyncController.php`, `src/Provider/Clock/ClockConfig.php`, `src/Core/MustBookingConfig.php`, `src/Admin/SettingsPage.php`, `src/Admin/SettingsDiagnostics.php`, `tests/ClockInboundWebhookSnsTest.php`, `tests/E2E/production-lifecycle-harness.php`, `tests/E2E/production-lifecycle-runner.php`, `docs/INTEGRATIONS.md`, `docs/developer/clock-provider-setup.md`, `docs/PROJECT_CONTEXT.md`, `docs/TASK_LOG.md`.
- Findings: Clock PMS PUSH webhooks are Amazon SNS envelopes and may use HTTP Basic credentials embedded in the endpoint URL. The previous inbound implementation treated a custom token/HMAC shared secret as the primary Clock webhook auth model, which is incompatible with official Clock PUSH.
- Fixes completed: Added SNS envelope parsing, SNS `SubscriptionConfirmation` and `Notification` handling, safe Amazon SNS `SigningCertURL` and `SubscribeURL` validation, SNS signature verification, SNS `Subject`/`Message` extraction, optional Clock PUSH Basic auth, and safe request summaries that avoid credentials/signatures/full payloads. Kept legacy Bearer token and HMAC auth for backward compatibility and preserved provider-log idempotency. Updated settings wording and readiness gates so missing `clock_webhook_secret` no longer blocks official Clock PUSH/SNS.
- Commands/tests run: `php -l src\Provider\Clock\ClockInboundSyncController.php`; `php -l src\Provider\Clock\ClockConfig.php`; `php -l src\Core\MustBookingConfig.php`; `php -l src\Admin\SettingsPage.php`; `php -l src\Admin\SettingsDiagnostics.php`; `php -l tests\ClockInboundWebhookSnsTest.php`; `php -l tests\E2E\production-lifecycle-harness.php`; `php -l tests\E2E\production-lifecycle-runner.php`; `php tests\ClockInboundWebhookSnsTest.php`; `php tools\lifecycle-sync-smoke-test.php`; `php tests\E2E\production-lifecycle-harness.php --correlation-id=MHB-E2E-20260614-CLOCKSNS`.
- Exact results: All listed PHP lint checks passed. `php tests\ClockInboundWebhookSnsTest.php` returned `Clock inbound SNS replay tests passed.` and covered valid/invalid Basic auth, valid/invalid SNS signatures, subscription confirmation, normal notification processing, and duplicate replay. Lifecycle smoke test returned `PASS`. Read-only production lifecycle harness returned `overall_status=BLOCKED` only because write scenarios require `--allow-external-writes`; `credentials.clock_inbound_auth` returned `PASS` with `sns_signature_verification=true`, `basic_auth_set=false`, and `legacy_token_or_hmac_set=false`.
- External records created: None. The replay suite uses local stubs and does not write to Clock, Stripe, PokPay, or WordPress database tables.
- Remaining risks/blockers: Do not claim production Clock inbound synchronization complete until the live/sandbox Clock PUSH endpoint is configured, any optional endpoint Basic credentials are set in WordPress, and real Clock SNS replay testing passes.

## 2026-06-14 - Production release preparation for 0.4.80
- Phase status: BLOCKED only for Clock inbound synchronization replay; production package preparation passed local checks.
- Files changed: `must-hotel-booking.php`, `readme.txt`, `src/Engine/PaymentEngine.php`, `docs/DATABASE.md`, `docs/INDEX.md`, `docs/PROJECT_CONTEXT.md`, `docs/TASK_LOG.md`.
- Findings: `public_callback_base_url` is a database/site option with an empty code default, so the local ngrok value is not packaged as a release default. Runtime scans found E2E identifiers only in docs/tests/evidence/tooling; tests, docs, tools, tmp, dev metadata, backups, logs, existing ZIPs, and VCS files are excluded from the production ZIP. The live callback base URL required after install is `https://new.empirebeachresort.al`.
- Fixes completed: Bumped plugin version/stable tag to `0.4.80`, updated the Stripe environment catalog production example to `https://new.empirebeachresort.al`, and removed the literal local callback URL example from the local environment catalog.
- Commands/tests run: release-prep scans and diff review; `php -l` across 289 PHP files; `php tools/lifecycle-sync-smoke-test.php`; `php tests/E2E/production-lifecycle-harness.php --correlation-id=MHB-E2E-20260614-RCPREP`; `php tools/clock-accounting-readiness.php`; `php tools/clock-accounting-environment-report.php`; clean ZIP build and ZIP content inspection.
- Exact results: PHP lint passed for 289 files. Lifecycle smoke test returned `PASS`. Read-only E2E harness returned `overall_status=BLOCKED` only because the Clock webhook secret is missing and write scenarios require `--allow-external-writes`; readiness checks passed for backup, non-production local environment, Clock sandbox, PokPay staging, Stripe non-live credentials, Stripe webhook secret, public HTTPS callbacks, and Clock deposit accounting. Production ZIP `must-hotel-booking-0.4.80.zip` contains 338 entries and excludes docs, tests, tools, tmp, dev metadata, backups, logs, `.git`, and nested ZIP files. ZIP scan found no ngrok URL, E2E IDs, sandbox provider IDs, Stripe-style keys, webhook secrets, bearer/basic tokens, or old `https://empirebeachresort.al` URL.
- External records created: None. This release-prep phase performed no provider, Clock, or payment writes.
- Remaining risks/blockers: Configure a production Clock webhook secret and replay an inbound Clock webhook before claiming Clock inbound synchronization complete.

## 2026-06-14 - Write-enabled E2E lifecycle run completed
- Phase status: BLOCKED only for inbound Clock webhook replay because the Clock webhook secret is missing. Stripe test-mode, PokPay staging, outbound Clock sandbox booking/accounting, local payment/refund ledgers, cancellation separation, and duplicate callback/webhook idempotency passed with fresh disposable records.
- Files changed: `tests/E2E/production-lifecycle-harness.php`, `tests/E2E/production-lifecycle-runner.php`, `src/Database/ClockFolioAccountingRepository.php`, `src/Database/PaymentRepository.php`, `src/Engine/BookingStatusEngine.php`, `src/Engine/PaymentEngine.php`, `docs/TASK_LOG.md`.
- Findings: The existing harness still only reported readiness, so a resumable write-enabled runner was added under `tests/E2E/`. The runner creates Clock-backed pending reservations through the active provider flow, starts fresh Stripe/PokPay checkouts through `PaymentEngine::processPayment()`, stores sanitized evidence in `tests/E2E/evidence/`, and resumes verification after manual checkout.
- Findings: The first paid Stripe replay exposed a real idempotency defect: after a refund, duplicate `checkout.session.completed` could update the latest Stripe refund ledger row back to `paid`, reconfirm the reservation, and post a second Clock deposit. The first refund-accounting pass also exposed that refund sync did not treat future-stay `deposit` accounting rows as valid original payment folios.
- Fixes completed: The requested command now performs sandbox/staging writes when provider-specific non-production guards pass. It does not require the Clock webhook secret for outbound Clock booking/accounting checks. Clock refund matching now accepts original `payment` or `deposit` accounting rows. Stripe completed-session handling now no-ops when the exact payment transaction is already recorded as paid, and payment-row updates prefer exact provider transaction matches before falling back to the latest method row.
- Commands/tests run: `php -l tests/E2E/production-lifecycle-harness.php`; `php -l tests/E2E/production-lifecycle-runner.php`; `php -l src/Database/ClockFolioAccountingRepository.php`; `php -l src/Database/PaymentRepository.php`; `php -l src/Engine/BookingStatusEngine.php`; `php -l src/Engine/PaymentEngine.php`; `php tools/lifecycle-sync-smoke-test.php`; escalated `php tests\E2E\production-lifecycle-harness.php --allow-external-writes --correlation-id=MHB-E2E-20260614-FULL` multiple times after manual checkout and fixes; escalated compact ledger/Clock folio reread through `tmp/codex-e2e-inspect.php`.
- Exact results: Final harness returned exit code `0` and `overall_status=BLOCKED` only because `clock.inbound_webhook_replay` is blocked by the missing Clock webhook secret. Clean Stripe replacement reservation `135` passed provider payment, signed webhook, local ledger, Clock booking, positive Clock deposit, duplicate checkout replay, cancellation, provider refund, refund webhook, negative Clock refund item, duplicate refund replay, and final folio balance `0`. PokPay reservation `134` passed staging auth, fresh SDK order, manual payment, local callback/webhook update, provider refund, Clock positive/negative accounting, duplicate payment/refund webhook replay, and final folio balance `0`.
- External records created: Contaminated first Stripe local reservation `133`, local booking `MHB-20260614-4417F113`, Clock booking/reservation `36600645`, Stripe Checkout Session `cs_t...bnOI`, Stripe refund `re_3...wdID` archived after the duplicate replay defect was fixed; clean Stripe local reservation `135`, local booking `MHB-20260614-D7979E7C`, Clock booking/reservation `36600950`, Stripe Checkout Session `cs_t...qpAt`, payment row `65`, refund row `15`, Clock folio `71474148`, positive credit item `60085760`, negative credit item `60085845`, Stripe refund `re_3...teub`, amount `150 EUR`; PokPay local reservation `134`, local booking `MHB-20260614-574A7198`, Clock booking/reservation `36600654`, SDK order `74e9...0604`, payment row `61`, refund row `13`, Clock folio `71473486`, positive credit item `60085197`, negative credit item `60085435`, PokPay refund `8d6...fe1b`, amount `150 EUR`.
- Remaining risks/blockers: Clock inbound webhook replay remains blocked by missing Clock webhook secret only. The contaminated first Stripe sandbox reservation `133` remains as audit evidence and should not be used as final acceptance evidence. No production ZIP/release was built after this write-enabled run.

## 2026-06-14 - Production readiness continuation with ngrok callback base
- Phase status: BLOCKED for full production-ready acceptance; READY for Stripe/PokPay/outbound Clock write-enabled E2E prerequisites. The only remaining configuration blocker found in this pass is the missing Clock inbound webhook secret, which blocks Clock inbound webhook replay only.
- Files changed: `tests/E2E/production-lifecycle-harness.php`, `tools/clock-accounting-environment-report.php`, `docs/INTEGRATIONS.md`, `docs/TROUBLESHOOTING.md`, `docs/TASK_LOG.md`.
- Findings: `public_callback_base_url` was still blank even though an ngrok tunnel was active. After setting it to `https://headwear-cranium-daydream.ngrok-free.dev`, Stripe, PokPay, and Clock webhook URLs are generated as public HTTPS URLs. Clock webhook secret remains unset, but it is not required for outbound Clock API/accounting tests.
- Findings: Read-only network preflight with external network access reached Clock sandbox booking fetch, booking folio list, folio fetch, and payment subtype endpoints; Stripe test/staging auth probe passed. PokPay staging was reachable, but the latest historical SDK order returned `Order not found`, so PokPay payment/refund E2E needs a fresh staging order.
- Fixes completed: Updated the production lifecycle harness to split provider-specific readiness gates. Stripe, PokPay, outbound Clock positive/negative accounting, and duplicate Stripe/PokPay callback groups can become `READY` without a Clock inbound webhook secret. Clock inbound webhook replay remains separately `BLOCKED`. Fixed the Clock accounting environment report to read the active Stripe environment webhook secret instead of the legacy production mirror field.
- Commands/tests run: `php tools/clock-e2e-backup.php`; `php tools/clock-e2e-settings.php --public-callback-base-url=https://headwear-cranium-daydream.ngrok-free.dev`; `php tests/E2E/production-lifecycle-harness.php --allow-external-writes --correlation-id=MHB-E2E-20260614-PRENGROK`; `php tests/E2E/production-lifecycle-harness.php --allow-external-writes --correlation-id=MHB-E2E-20260614-NGROKREADY`; `php tests/E2E/production-lifecycle-harness.php --allow-external-writes --correlation-id=MHB-E2E-20260614-SPLITGATES`; escalated read-only `php tools/provider-preflight-report.php`; `php -l tests/E2E/production-lifecycle-harness.php`; `php -l tools/clock-accounting-environment-report.php`; `php tools/clock-accounting-environment-report.php`; escalated unsigned public POST to the Stripe webhook URL; `php tools/lifecycle-sync-smoke-test.php`.
- Exact results: Fresh ignored backup created at `tools/backups/clock-e2e-20260614-115627` with 76 reservations, 42 payments, 9 refunds, 28 Clock accounting rows, 8334 provider logs, 8 sync jobs, and 89 mappings. After setting the callback base, harness callback checks passed. After harness gate split, `stripe.payment_lifecycle`, `stripe.refund_lifecycle`, `pokpay.payment_lifecycle`, `pokpay.refund_lifecycle`, `clock.positive_accounting`, `clock.negative_accounting`, and `duplicates.webhooks_and_callbacks` returned `READY`; `clock.inbound_webhook_replay` returned `BLOCKED` because Clock webhook secret is missing. Unsigned public Stripe webhook POST returned HTTP 400 with `Invalid Stripe signature`, proving ngrok reaches the active plugin route and the handler executes. Lifecycle smoke test returned `PASS`.
- External records created: None. This pass changed a local option and created a local ignored backup only; it did not create new bookings, Stripe sessions/refunds, PokPay orders/refunds, Clock records, payment rows, refund rows, or accounting rows.
- Remaining risks/blockers: Full production-ready acceptance still requires actual write-enabled Stripe/PokPay/Clock E2E records and rereads, plus Clock inbound webhook secret for inbound replay. PokPay staging checkout may require manual browser interaction after a fresh staging order is created.

## 2026-06-14 - Production readiness Phase 6 cleanup, tests, and release candidate
- Phase status: BLOCKED for final production-ready acceptance. Local cleanup, smoke checks, package build, and documentation updates passed, but required external E2E, fresh install/activation/migration/uninstall verification, and provider callback replay remain blocked by configuration/environment prerequisites.
- Files changed: `docs/TASK_LOG.md`, `docs/DECISIONS.md`.
- Findings: No Composer, npm, or PHPUnit config is present, so available automated verification is PHP syntax, existing CLI smoke tools, the new E2E readiness harness, static scans, and local package inspection. The existing `tools/release-plugin.ps1` remains unsafe for this task because it pulls, commits, pushes, tags, and creates a GitHub release; it was not used.
- Fixes completed: Built a local release-candidate ZIP manually from a temporary staging folder, excluding `.git`, `.agents`, `.codex`, `tmp`, `tests`, `tools/backups`, existing ZIPs, and `.gitignore`. Removed the temporary staging folder after verifying it was inside the plugin workspace. Updated `docs/DECISIONS.md` with the manual-review financial mismatch policy.
- Commands/tests run: `php -l must-hotel-booking.php`; `php -l src/Provider/Clock/ClockPaymentReconciliationService.php`; `php -l src/Admin/ReservationAdminDataProvider.php`; `php -l src/Admin/reservations.php`; `php -l tools/provider-preflight-report.php`; package build through `Compress-Archive`; ZIP content inspection with `System.IO.Compression.ZipFile`; `php tests/E2E/production-lifecycle-harness.php --allow-external-writes --correlation-id=MHB-E2E-20260614-FINALDRY`; `git diff --stat`; `git status --short`.
- Exact results: PHP syntax checks passed on the changed and entry-point PHP files. Release ZIP created at `must-hotel-booking-0.4.79-local-20260614-004106.zip` with 391 entries and size 2,258,091 bytes. ZIP inspection returned no entries matching `tools/backups`, `tests`, `tmp`, `.git`, or nested `.zip` paths. Final write-requested harness returned `overall_status=BLOCKED` and `external_writes_performed=false`; backup, staging/site mode, Clock sandbox, PokPay staging, Stripe non-live, Stripe/PokPay/Clock credentials, Stripe webhook secret, and Clock deposit accounting checks passed; Clock webhook secret and public HTTPS callback URLs remained blocked.
- External records created: None. Phase 6 performed no external writes and created no local bookings, provider sessions/orders/refunds, Clock records, payment rows, refund rows, or accounting rows.
- Remaining risks/blockers: Final status cannot be `PRODUCTION READY`. Required external Stripe/PokPay/Clock E2E, duplicate webhook/callback replay, retry/reconciliation simulation against provider rereads, fresh install/activation/migration/deactivation/reactivation/uninstall verification, and full schema/uninstall safety checks remain blocked or unavailable until public HTTPS callback base URL, Clock webhook secret, and a disposable install/test environment are provided.

## 2026-06-14 - Production readiness Phase 5 security, schema, config, and observability
- Phase status: BLOCKED for external security/E2E replay; local security hardening and static checks passed. Public HTTPS callback URLs are still unavailable and Clock webhook secret is missing, so signed Clock replay, provider callback replay, and full external observability validation cannot run.
- Files changed: `src/Admin/PaymentAdminActions.php`, `docs/TASK_LOG.md`.
- Findings: REST routes are concentrated in payment callbacks, Clock inbound sync, and support diagnostics. Stripe verifies webhook signatures; PokPay finalize uses REST nonce and server-side order verification; PokPay webhook verifies by server-side order fetch; Clock webhook requires token/signature when a secret is configured and dedupes successful events through provider request logs. Support diagnostics is token-protected and no-cache.
- Findings: Payment admin write actions had nonce checks and were normally reached through a `manage_options` admin page, but the action handler did not enforce the capability directly before refund/accounting/payment write branches.
- Fixes completed: Added direct `manage_options` enforcement in `PaymentAdminActions` for GET and POST payment write handling, so refund, payment status, Clock accounting retry, manual accounting completion, and payment settings saves are guarded inside the write handler as well as by the page shell.
- Commands/tests run: targeted REST/admin/security/schema searches; scoped secret scan excluding backups/vendor/lib/zips; `php -l src/Admin/PaymentAdminActions.php`; `php -l src/Core/SupportDiagnosticsEndpoint.php`; `php -l src/Provider/Clock/ClockInboundSyncController.php`; `php tools/lifecycle-sync-smoke-test.php`; `php tests/E2E/production-lifecycle-harness.php`; `git diff --stat`; `git status --short`.
- Exact results: PHP syntax checks passed on the changed/inspected Phase 5 PHP files. Lifecycle sync smoke test returned `PASS`. Secret scan found only redaction/detection patterns in `SupportDiagnosticsEndpoint` and the E2E harness, not embedded live credentials. E2E harness returned `overall_status=BLOCKED`, with local backup/non-production/credential checks passing except Clock webhook secret and public HTTPS callback URLs.
- External records created: None. Phase 5 performed no external writes and created no local bookings, provider sessions/orders/refunds, Clock records, payment rows, refund rows, or accounting rows.
- Remaining risks/blockers: Full CSRF/replay verification for public callbacks/webhooks and provider failure callbacks still needs public HTTPS callback configuration and Clock webhook secret. Broader destructive schema/uninstall tests are deferred to Phase 6 after release-candidate cleanup checks.

## 2026-06-14 - Production readiness Phase 4 defect fixes, retry, and reconciliation
- Phase status: BLOCKED for external retry/reconciliation E2E; local retry and accounting hardening passed. The external blockers are unchanged: public HTTPS callback URLs are unavailable and Clock webhook secret is missing.
- Files changed: `src/Provider/Storage/ProviderSyncJobRepository.php`, `src/Provider/Clock/ClockPaymentAccountingService.php`, `docs/INTEGRATIONS.md`, `docs/PAYMENTS.md`, `docs/TASK_LOG.md`.
- Findings: Existing provider sync jobs already support atomic claims, stale-lock release, backoff, max attempts, cron processing, admin-triggered manual runs, and `enqueueOnce` idempotency. Clock accounting already guards duplicate posts by idempotency key and posting claim, and admin payment detail can retry failed/manual-review accounting rows or mark them handled manually.
- Findings: Two safety gaps remained. Stale running provider jobs were requeued without consuming an attempt, so repeated worker crashes could avoid terminal exhaustion. Ambiguous Clock folio/accounting target failures could stay as generic `failed` rows instead of being clearly terminal manual-review financial mismatches.
- Fixes completed: Stale running provider jobs now increment attempts when released, and move to `retryable` only if another attempt remains; otherwise they become `exhausted`. Ambiguous Clock payment folio selection failures and refund original-folio validation failures now mark Clock accounting and mirrored refund sync state as `manual_review`, not automatic repair candidates.
- Commands/tests run: `php -l src/Provider/Storage/ProviderSyncJobRepository.php`; `php -l src/Provider/Clock/ClockPaymentAccountingService.php`; `php tools/lifecycle-sync-smoke-test.php`; `php tests/E2E/production-lifecycle-harness.php`.
- Exact results: PHP syntax checks passed on changed Phase 4 PHP files. Lifecycle sync smoke test returned `PASS`. E2E harness returned `overall_status=BLOCKED`, with all local/non-production/config credential checks passing except Clock webhook secret and public HTTPS callback URLs.
- External records created: None. Phase 4 performed no external writes and created no local bookings, provider sessions/orders/refunds, Clock records, payment rows, refund rows, or accounting rows.
- Remaining risks/blockers: Provider retry and reconciliation duplicate/failure behavior still needs external replay against Stripe test mode, PokPay staging, and Clock sandbox once callbacks and Clock webhook secret are ready. No automatic repair was added for ambiguous financial mismatches; staff must resolve manual-review rows according to hotel policy.

## 2026-06-14 - Production readiness Phase 3 cancellation and amendment verification
- Phase status: BLOCKED for external cancellation/amendment E2E; local audit and code safety fix passed. The remaining blockers are external/configuration-only: public HTTPS callback URLs are still unavailable and Clock webhook secret is missing, so signed Clock/provider callback replay and full Clock amendment verification cannot safely run.
- Files changed: `src/Provider/Clock/ClockPaymentReconciliationService.php`, `src/Admin/ReservationAdminDataProvider.php`, `src/Admin/reservations.php`, `docs/BOOKING_FLOW.md`, `docs/TASK_LOG.md`.
- Findings: Cancellation and refund are already separated in current code. Clock/provider cancellations route through `BookingLifecycleSyncService`, which can create refund-review rows without automatically calling Stripe/PokPay. Explicit provider refunds remain handled by `PaymentRefundService`. Clock-backed stay-date amendment support already exists through `ClockPaymentReconciliationService::updateStayDates()` and is exposed through admin/staff flows.
- Findings: Stay-date amendments validate date order, check-in timing, reservation status, provider booking references, local availability, and Clock sync. The gap was financial adjustment state: when Clock/provider pricing changed after an amendment, the plugin refreshed totals but did not explicitly mark increased totals for additional-payment review or reduced totals for refund/credit review.
- Fixes completed: Added manual-review metadata for amendment price deltas. Increased totals now record `additional_payment_review_required`; reduced totals now record `refund_or_credit_review_required`; both set `manual_review_required` and surface in the admin provider mirror panel. No automatic charges, refunds, or Clock negative accounting entries were introduced.
- Commands/tests run: `php -l src/Provider/Clock/ClockPaymentReconciliationService.php`; `php -l src/Admin/ReservationAdminDataProvider.php`; `php -l src/Admin/reservations.php`; `php tools/lifecycle-sync-smoke-test.php`; `php tests/E2E/production-lifecycle-harness.php`.
- Exact results: PHP syntax checks passed on all changed Phase 3 PHP files. Lifecycle sync smoke test returned `PASS`. E2E harness returned `overall_status=BLOCKED`, with backups, staging/site mode, Clock sandbox, PokPay staging, Stripe non-live credentials, Stripe/PokPay/Clock credentials, Stripe webhook secret, and Clock deposit accounting passing; Clock webhook secret and public HTTPS callback checks remained blocked. No external writes were performed.
- External records created: None. Phase 3 performed no external writes and created no provider, Clock, payment, refund, accounting, or local booking records.
- Remaining risks/blockers: Actual amendment E2E for check-in change, check-out change, extension, shortening, supported room-type change, Clock reread comparison, duplicate amendment replay, and Clock failure consistency remains blocked until public HTTPS callbacks and Clock webhook secret are configured. Room-type amendment support still needs provider-specific confirmation before implementation; no automatic financial adjustment policy exists and must remain manual review until the business rule is defined.

## 2026-06-14 - Production readiness Phase 2 E2E harness and lifecycle verification gate
- Phase status: BLOCKED for external lifecycle execution; harness creation and local readiness checks passed. The remaining blockers are external/configuration-only: Clock webhook secret is missing and Stripe/PokPay/Clock callback URLs currently resolve to `localhost` because public callback base URL is blank.
- Files changed: `tests/E2E/production-lifecycle-harness.php`, `tests/E2E/README.md`, `tools/provider-preflight-report.php`, `docs/TASK_LOG.md`, `docs/FILE_MAP.md`, `docs/TROUBLESHOOTING.md`.
- Findings: No existing `tests/` structure was present. External lifecycle writes cannot safely start until the public HTTPS callback base and Clock webhook secret are configured. Existing active Stripe staging webhook secret is set; the old provider preflight incorrectly checked the legacy production mirror field instead of the active environment runtime secret.
- Fixes completed: Added a read-only-by-default E2E harness with `MHB-E2E-{date}-{unique}` correlation IDs, backup checks, non-production guards, provider credential checks, public callback checks, lifecycle PASS/BLOCKED/FAIL reporting, and `--allow-external-writes` dry-run gating. Fixed `tools/provider-preflight-report.php` to use `PaymentEngine::getStripeWebhookSecret()` and require public HTTPS URLs for provider callbacks.
- Commands/tests run: `php -l tests/E2E/production-lifecycle-harness.php`; `php tests/E2E/production-lifecycle-harness.php`; `php tests/E2E/production-lifecycle-harness.php --allow-external-writes --correlation-id=MHB-E2E-20260614-PHASE2DRY`; `php -l tools/provider-preflight-report.php`; escalated read-only `php tools/provider-preflight-report.php`.
- Exact results: Harness syntax passed. Harness read-only result: `overall_status=BLOCKED`, with backup, site staging, Clock sandbox, PokPay staging, Stripe non-live, Stripe/PokPay/Clock credentials, Stripe webhook secret, and Clock deposit accounting checks passing; Clock webhook secret and public HTTPS callback checks blocked. Write-requested dry run also returned `BLOCKED` and performed no external writes. Updated provider preflight syntax passed and read-only preflight reached Clock sandbox, Stripe, and PokPay staging; blockers are now Clock webhook secret plus non-public Clock/Stripe/PokPay URLs.
- External records created: None. Phase 2 performed no external writes and created no provider records.
- Remaining risks/blockers: Full Stripe, PokPay, Clock positive/negative accounting, refund, and duplicate callback/webhook E2E remain blocked until public HTTPS callbacks are configured and Clock webhook secret is set. PokPay staging checkout may still require manual browser completion once writes are enabled.

## 2026-06-14 - Production readiness Phase 1 baseline audit, backups, and preflight
- Phase status: BLOCKED for external E2E writes; code/local baseline checks may continue. The blockers are external/configuration-only: Clock webhook secret is missing and Stripe/PokPay/Clock callback URLs currently resolve to `localhost` because public callback base URL is blank.
- Files changed: `docs/TASK_LOG.md`, `docs/PROJECT_CONTEXT.md`, `docs/DATABASE.md`, `docs/INDEX.md`.
- Findings: Worktree was clean before edits. Current plugin version is `0.4.79`. Release script `tools/release-plugin.ps1` performs pull/commit/push/tag/GitHub release and is not authorized for this task; only a future local ZIP build is allowed. `.gitignore` excludes `tools/backups/` and `must-hotel-booking-*.zip`.
- Findings: REST/write surface includes Stripe/PokPay payment routes in `PaymentEngine`, Clock webhook in `ClockInboundSyncController`, support diagnostics route/settings action, admin `admin_init` handlers, public/staff/admin AJAX, lock cleanup cron, provider sync cron, and Clock auto-sync cron.
- Findings: Existing lifecycle services cover booking/status updates, refund review for Clock/provider cancellations, explicit Stripe/PokPay refunds, Clock deposit/payment/refund accounting, provider sync retry claims/backoff/stale-lock release, and Clock-backed stay-date amendments with pricing reconciliation flags.
- Findings: Stripe webhook signatures are verified, Clock webhook supports token/signature auth plus request-log dedupe, and PokPay callback/webhook paths confirm server-side by fetching the SDK order. Stripe event IDs are not persisted as a first-class event ledger; duplicate Stripe payment/refund replay must be proven by E2E/local tests before production readiness.
- Backups: Created current ignored DB/options/table backup at `tools/backups/clock-e2e-20260613-222640` with 76 reservations, 41 payments, 6 refunds, 27 Clock accounting rows, 8314 provider logs, 4 provider sync jobs, and 89 provider mappings. Created tracked-plugin snapshot `tools/backups/plugin-files-phase1-20260614-HEAD.zip`. These files are ignored and must be excluded from release ZIPs.
- Commands/tests run: `git status --short` clean before edits; `git diff --stat`; documentation reads; `rg --files`; targeted route/schema/lifecycle searches; `php -l` on provider/preflight and backup tools; `php tools/lifecycle-sync-smoke-test.php`; `php tools/provider-preflight-report.php` inside sandbox and then escalated read-only network; `php tools/clock-accounting-readiness.php`; `php tools/clock-accounting-environment-report.php`; backup creation commands.
- Exact results: PHP syntax checks passed for `tools/provider-preflight-report.php`, `tools/lifecycle-sync-smoke-test.php`, `tools/clock-e2e-backup.php`, `tools/clock-accounting-readiness.php`, and `tools/clock-accounting-environment-report.php`. Lifecycle smoke test returned `PASS`. Sandboxed provider preflight hit cURL error 7 for external hosts; escalated read-only preflight reached Clock sandbox booking/folio/payment subtype reads, Stripe auth probe, and PokPay staging SDK order fetch. A Phase 2 follow-up corrected provider preflight to check the active Stripe webhook secret, which is configured.
- External records created: None. Phase 1 performed no external writes. Read-only preflight referenced existing local reservation `132`, Clock booking `3659****`, Stripe latest reference availability, and PokPay latest SDK order availability without creating records.
- Remaining risks/blockers: Cannot run signed Clock webhook E2E or provider callback E2E until a public HTTPS callback base URL is configured and Clock webhook secret is set. Clock write permissions remain not verified by read-only preflight. PokPay refund endpoint access remains not verified by read-only preflight.

## 2026-06-13 - Provider cancellation lifecycle completion and preflight
- Changed: Extended `BookingLifecycleSyncService` so paid Stripe/PokPay bookings cancelled from Clock/provider sync create one `refund_review_required` / `clock_cancellation_review` row when no active/completed refund already exists.
- Changed: Added admin payment warning/list metadata for refund-review rows, a standalone lifecycle smoke test, and a read-only provider preflight report for Clock/Stripe/PokPay.
- Verified: Lifecycle smoke test passes for paid Stripe Clock cancellation idempotency: one cancellation hook and one refund-review row with provider-fee-aware amount.
- Verified: Read-only provider preflight reached Clock sandbox booking fetch/folio endpoints, Stripe balance auth, and PokPay staging SDK-order fetch with configured credentials.
- Blocked: Full destructive live provider E2E is still blocked by missing Clock webhook secret, missing Stripe webhook secret, and webhook URLs resolving to localhost because public callback base is blank.
- Checks: PHP syntax checks passed on changed PHP files and CLI tools.

## 2026-06-13 - Clock cancellation lifecycle routing
- Changed: Added `BookingLifecycleSyncService` and routed Clock inbound webhook/refresh status changes, Clock booking upsert status changes, and successful outbound Clock cancellation reconciliation through it.
- Fixed: Clock-originated cancellations no longer update only the reservation repository; they now pass through `BookingStatusEngine::updateReservationStatuses()` so cancellation hooks and email handlers run once when the status first changes to `cancelled`.
- Limits: No live Clock/PokPay/Stripe E2E was run in this pass; provider credentials and callback reachability still need preflight before destructive sandbox tests.
- Checks: PHP syntax checks passed on changed PHP files.

## 2026-06-13 - PokPay automatic refunds and Clock deposit posting
- Changed: Re-enabled PokPay automatic refunds using the documented merchant SDK-order refund endpoint and retained `manual_pending` only when PokPay does not confirm the refund.
- Changed: Added the verified Clock deposit workflow for future online payments: create or reuse a booking folio with `deposit=true`, then post the Stripe/PokPay external provider payment as a credit item on that deposit folio.
- Verified: PokPay staging SDK order `8b29da85-881a-4e05-b4de-2dc9180ad309` refunded successfully and fetched back as `REFUNDED`; duplicate refund returned a controlled provider failure/manual fallback.
- Verified: Clock sandbox booking `36591448` received deposit folio `71442250`, deposit payment credit item `60052192`, duplicate payment sync returned `already_posted`, and deposit refund/reversal credit item `60052222` returned the deposit folio balance to zero.
- Limits: Clock UI Required Deposit / Deposit Payment fields were not visually verified because no dashboard browser session was available; the result is based on official API docs/support behavior plus API read-back.
- Checks: PHP syntax checks passed on changed PHP files, and the Clock accounting readiness/environment report showed sandbox/test configuration with deposit endpoints configured.

## 2026-06-13 - PokPay refund and Clock deposit audit follow-up
- Changed: Corrected stale staff portal/developer docs that still described PokPay automatic refunds through the guessed `sdk-orders/{orderId}/refund` path.
- Evidence: Git history shows releases `v0.4.74`/`v0.4.75` attempted that path, and local refund row `5` was marked succeeded with a provider refund UUID, but PokPay requests were not logged and official docs still do not publish a refund endpoint or contract.
- Clock: Official Clock collection exposes `booking_folio - CREATE` with `deposit=true` and generic folio `credit_items`, but no verified API sequence for an already-captured website payment to appear as Required Deposit / Deposit Payment without settling the normal folio balance.
- Superseded: Later verification in this task found the public PokPay Postman/API refund endpoint and verified the Clock deposit-folio workflow.

## 2026-06-13 - PokPay refund API safety correction
- Changed: Stopped calling the unverified `merchants/{merchantId}/sdk-orders/{orderId}/refund` path and made PokPay refunds manual-required by default.
- Reason: Official PokPay docs verified in this pass list SDK order create/fetch/confirm/capture endpoints, but no refund endpoint, payload, status polling, webhook, or idempotency behavior.
- Behavior: PokPay refund requests now create `manual_pending` refund records; staff must refund in the POK dashboard and mark the refund completed in the plugin before local refund ledger/cancel-after-refund/Clock refund accounting continue.
- Checks: PHP syntax checks were run on the touched PHP files. Full provider sandbox payment/refund E2E was not run in this pass.
- Superseded: Later verification in this task found PokPay's public Postman/API refund endpoint and restored automatic-first refunds with manual fallback.

## 2026-06-13 - Public callback base and Clock/payment E2E attempt
- Changed: Added application-level E2E backup/settings/cleanup CLI helpers, optional public callback base URL support, public-host URL rewriting for provider callback/tunnel requests, and admin/diagnostic display of public Stripe/PokPay/Clock callback URLs.
- Verified: Backup `tools/backups/clock-e2e-20260613-152054` was created before option mutation; public callback base `https://headwear-cranium-daydream.ngrok-free.dev` generated Stripe/PokPay/Clock webhook URLs; emulated public-host booking HTML returned no `localhost` references; local booking -> accommodation -> checkout -> confirmation reached Stripe Checkout with real staging/sandbox credentials.
- Test artifact: Stripe session `cs_test_a1ritVtK4EDrQuinXs6a3DLSrDOPRaIhhKAAZTigeBzMvZd4Ep8tvLkyYg` created local reservation `128` / booking `MHB-20260613-263D7D6F` / Clock sandbox booking `36591448`; payment was not completed.
- Cleanup: Local reservation `128` was cancelled and payment `54` marked failed via `tools/clock-e2e-cleanup.php`; public callback base was restored to blank. Clock cancellation was queued as provider sync job `4` because CLI Clock API access failed with cURL error 7 in the sandbox.
- Blocked: Full paid Stripe/PokPay/Clock accounting E2E was not completed. Remaining blockers are unsandboxed/public network access for provider callbacks/Clock API retries, missing Stripe webhook secret for true webhook verification, and the still-unverified Clock booking deposit/prepayment endpoint for future-stay accounting.

## 2026-06-13 - Clock accounting E2E environment probe
- Changed: Added `tools/clock-accounting-environment-report.php`, a CLI-only read-only report for sanitized site, Clock, payment, recent reservation, payment, and Clock accounting state.
- Verified: Clock is configured as `sandbox`; provider mode is `clock`; checkout can offer `pokpay` and `stripe`; active site environment is `staging`; active ngrok public URL `https://headwear-cranium-daydream.ngrok-free.dev` reaches the local WordPress site when public network access is allowed.
- Blocked: WordPress still generates REST/webhook URLs as `http://localhost:10016/...`, `mysqldump` is not available in the shell, and no full DB backup was created before URL/option mutation. Real provider callback E2E should not proceed until a backup exists and public callback URLs are restored after testing.
- Checks: Read-only readiness/report tools and invalid webhook reachability checks were run; Stripe invalid signature was rejected with a controlled `400`.

## 2026-06-13 - Clock accounting safety cleanup
- Changed: Replaced the unsafe temporary Clock E2E script with a CLI-only read-only readiness tool, added structured Clock accounting reason codes, rejected unsafe endpoint overrides, and added admin handling for payment accounting rows marked `handled_manually`.
- Notes: Deposit/prepayment automation remains intentionally blocked until Clock confirms the endpoint, method, payload, API area, rights, external reference/idempotency behavior, and refund/reversal flow.
- Checks: PHP syntax checks were run on changed PHP files. Endpoint validation and readiness checks were run read-only; DB-mutating accounting probes were not run.

## 2026-06-12 - Clock future-payment accounting guard
- Changed: Added `ClockEndpointRegistry`, endpoint override settings, Clock payment posting mode, diagnostics fields, and a conservative payment-accounting branch that stops future online payments from posting as folio `credit_items` unless a verified booking deposit/prepayment endpoint exists.
- Docs: Updated integration, payment, and troubleshooting notes with the Clock support question needed to verify external already-paid deposit/prepayment posting.
- Verification: PHP syntax checks were run on changed PHP files. No Clock sandbox payment test was run, so deposit automation is not claimed complete.

## 2026-06-12 - PokPay SDK checkout modes
- Changed: Added selectable PokPay SDK checkout modes: `embedded_sdk` renders the PokPay SDK container on the confirmation page, and `sdk_confirm_url_redirect` creates the same SDK order then redirects to the provider `sdkOrder._self.confirmUrl`.
- Notes: Both modes use `POST merchants/{merchantId}/sdk-orders` and only mark paid after server-side `GET sdk-orders/{orderId}` verification. The admin setting is `pokpay_checkout_mode` under Payments -> Payment Settings -> PokPay Checkout.
- Checks: PHP syntax checks for payment/admin/confirmation files and `node --check assets/js/booking-confirmation.js` passed. Full PokPay card payment still requires a manual staging checkout.

## 2026-06-11 - Documentation knowledge base setup
- Changed: Created/updated Codex project instructions and concise docs knowledge base.
- Notes: No behavior changed. Current local plugin header reports `0.4.72`; the request referenced inspected version `0.4.71`.
- Checks: No PHP lint required because only markdown files changed. Verify with `git diff --stat`.

## 2026-06-11 - Knowledge base hardening pass
- Changed: Tightened future workflow rules, added code-vs-docs trust guidance, known uncertain areas, targeted search recipes, and a dedicated staff portal doc.
- Notes: No behavior changed.
- Checks: No PHP lint required because only markdown files changed. Verify with `git diff --stat`.

## 2026-06-11 - Sandbox payment integration blockers
- Changed: Fixed Clock scalar folio ID parsing, nested Clock folio balance verification, documented Clock booking cancellation default, removed unsupported PokPay SDK order fields, and added no-op/claim guards for duplicate payment/refund processing.
- Tests: Stripe test checkout, webhook, Clock payment credit item, duplicate checkout event, Stripe refund, refund webhook resend, negative Clock refund credit item, and Clock cancellation passed against sandbox/demo services.
- Blocked: PokPay staging order creation reached the provider but failed with `Expired token`; refresh the active staging `pokpay_staging_key_id` / `pokpay_staging_key_secret` credentials before rerunning PokPay payment, duplicate finalize, refund, and Clock refund tests.

## 2026-06-12 - Refund and cancellation release-readiness layer
- Changed: Added Stripe/PokPay provider fee snapshots, PokPay fee estimate settings, default refund breakdown storage, admin fee/refund diagnostics, and a two-column customer cancellation page using booking confirmation styling.
- Notes: Stripe was already manually verified end-to-end before this layer. PokPay is code-ready but still needs refreshed staging/live credentials for manual payment/finalize/refund testing.
- Rules: Default refund is paid amount minus stored provider fee and cancellation fee. Inside the 21-day/default cancellation window, customer self-service cancellation/refund is blocked and handled by hotel/staff.
- Checks: PHP syntax checks on changed PHP files. No full live Stripe/PokPay/Clock payment flow was run.

## 2026-06-12 - Final PokPay/refund release verification
- Changed: Added current-version schema repair for payment fee/refund breakdown columns, tightened PokPay nested fee extraction, clarified PokPay diagnostics fee source, and adjusted customer cancellation review to left action panel plus right booking summary.
- Verified: Active staging PokPay credentials passed auth preflight; one staging SDK order smoke test succeeded without local booking/finalize/refund; DB sanity confirmed payment/refund columns and PokPay finalize/error routes.
- Notes: Local PokPay fee estimate settings are currently missing, so PokPay payments that do not return an API fee will correctly block automatic customer refund/cancellation pending manual review.
- Checks: PHP syntax checks on changed PHP files. No full Stripe/Clock browser test or full PokPay payment/refund flow was run.
# 2026-06-18 - Clock cancellation financial cleanup hardening
- Added immutable cancellation financial snapshots and separate reservation/refund/Clock cleanup states.
- Routed local admin, guest, and staff cancellation through the lifecycle service; provider-backed staff approval now uses Clock cancellation instead of a local-only status write.
- Prevented guest cancellation from marking WordPress cancelled while Clock cancellation is only queued.
- Added reread-first/re-read-after-write Clock cancellation verification.
- Added Stripe/PokPay duplicate refund and currency guards, reused cancellation review rows, and completed cancel-after-refund from Stripe webhook recovery.
- Added expected-versus-actual Clock folio balance fields and verification.
- Added admin/staff warnings for manual Clock accommodation-charge and cancellation-fee cleanup.
- Status: local code/tests pass; external provider acceptance not run. Clock accommodation-charge cleanup remains blocked pending an official contract.

## 2026-06-22 - PokPay diagnostics, Clock deposit isolation, and rate limiting
- Changed: Added no-charge PokPay credential verification/readiness states, masked admin reporting, secret preservation, provider-specific logs/timelines, and separation from Stripe errors.
- Changed: Hardened Clock deposit tuple idempotency, durable folio/item metadata, standard-folio isolation verification, and manual review for transferred/applied or legacy-folio refunds.
- Changed: Added recursive provider redaction, four-per-second Clock throttling including Digest calls, `Retry-After`/backoff/jitter, GET deduplication, and short safe catalogue caching.
- Evidence: Sanitized June 14 backup shows historical Stripe folio `71355798` had `deposit=false` and received the payment directly; later deposit flow created `deposit=true` folio `71442250` and posted credit item `60052192` there while the original standard folio ID remained separate.
- Blocked: Current local WordPress DB was unavailable on 2026-06-22, so current saved PokPay credentials and live Clock folio state could not be re-read. No real provider write was performed.
- Checks: PHP lint and standalone mocked tests cover PokPay environment/auth states, redaction, Clock deposit isolation/idempotency/refund review, 429 backoff, and duplicate GET suppression.
